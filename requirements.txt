torch>=2.4.0
transformers>=4.50.0
accelerate>=0.30.0
python-dotenv>=1.0.0

# Task 3+ (validation, rendering, VLM judge) — install when you get there:
# pytest>=8.0.0
# cairosvg>=2.7.1
# Pillow>=10.0.0
# openai>=1.0.0
